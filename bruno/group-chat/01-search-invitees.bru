meta {
  name: Search Invitees
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/group-chats/users/search?q=budi
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });

  test("users ada di response", function() {
    expect(res.body.users).to.be.an("array");
  });
}
