meta {
  name: Explore - Leaderboard
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/explore/leaderboard?limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });

  test("leaderboard ada di response", function() {
    expect(res.body.leaderboard).to.be.an("array");
  });
}
