meta {
  name: Explore - Like Post
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/explore/posts/{{explorePostId}}/like
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
