meta {
  name: Get Home Data
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/dashboard/home
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
