meta {
  name: Get Active Plan
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/training/active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });

  test("flag canGenerate ada di response", function() {
    expect(res.body.canGenerate).to.be.a("boolean");
  });
}
