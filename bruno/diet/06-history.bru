meta {
  name: Get Plan History
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/diet/history?limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });

  test("response berupa array", function() {
    expect(res.body).to.be.an("array");
  });
}
